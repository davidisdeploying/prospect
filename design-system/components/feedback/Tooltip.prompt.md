**Tooltip** — carries the plain meaning behind a mining term (the legibility rule); hover/focus reveals it.

```jsx
<Tooltip label="Applied"><Badge>Staked</Badge></Tooltip>
```

`side`: top | bottom | left | right.
