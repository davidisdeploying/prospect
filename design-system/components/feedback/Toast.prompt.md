**Toast** — confirmation in the interface's voice ("Claim staked," never "Submitted"); tone tints the left rule.

```jsx
<Toast message="Claim staked." tone="gold" action={<Button variant="quiet" size="sm">Undo</Button>} />
<Toast message="Moved to tailings." tone="danger" />
```
