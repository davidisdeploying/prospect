**Logo** — the gold-pan logomark, self-contained inline SVG. Nugget is the only gold.

```jsx
<Logo size={42} />                 // perspective pan
<Logo size={20} variant="compact" /> // favicon / small
```
